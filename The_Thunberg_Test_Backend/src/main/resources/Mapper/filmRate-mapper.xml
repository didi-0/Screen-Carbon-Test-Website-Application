<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.uog.thunbergTest.mapper.FilmRateMapper">

    <insert id="updateAndInsert">

        REPLACE INTO film_rate

        (score, film_id, film_type, rating_date)

        VALUES (#{score}, #{filmId}, #{filmType}, #{ratingDate})

    </insert>
</mapper>